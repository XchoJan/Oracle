/** Инструкции JSON для модели по типу отчёта. */

const BASE_RULES = `Стиль: холодный, без романтизации и вдохновляющих клише. Явно указывай неопределённость. Не диагностируй расстройства. Не обещай конкретных внешних событий.
Запрещено дословно перечислять ответы «да/нет» по пунктам. Синтезируй паттерны.
methodology_note: 2–3 предложения — вероятностная модель по самоотчёту, не медицина и не финсовет.`;

export const FREE_REPORT_INSTRUCTION = `Верни ТОЛЬКО валидный JSON:
{ "sections": [{ "title": "строка", "content": "строка", "recommendations": ["..."] }], "methodology_note": "строка" }

Ровно 3 секции. В каждой: content 3–5 предложений; recommendations — ровно 3 конкретные строки-действия.
${BASE_RULES}`;

export const PAID_DEEP_REPORT_INSTRUCTION = `Верни ТОЛЬКО валидный JSON:
{ "sections": [{ "title": "строка", "content": "строка", "recommendations": ["..."] }], "methodology_note": "строка" }

Ровно 5 секций. В каждой: content 4–6 предложений; recommendations — ровно 4 строки.
Горизонт анализа — 12–24 месяца, укажи это в тексте где уместно.
${BASE_RULES}`;

export const PAID_MAP24_INSTRUCTION = `Верни ТОЛЬКО валидный JSON:
{ "sections": [{ "title": "строка", "content": "строка", "recommendations": ["..."] }], "methodology_note": "строка" }

Минимум 7 секций, строго в порядке заголовков:
1. "Здоровье и соматический ресурс"
2. "Финансы и предсказуемость дохода"
3. "Работа, нагрузка и профессиональная траектория"
4. "Отношения и социальная среда"
5. "Психическое состояние, стресс и регуляция"
6. "Быт, структура и самоорганизация"
7. "Риски на горизонте 24 месяцев"
В каждой секции: content 4–7 предложений; recommendations — ровно 6 строк.
${BASE_RULES}`;
