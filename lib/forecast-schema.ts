export type ForecastSection = {
  title: string;
  /** Синтез сценария и рисков; не пересказ ответов «да/нет» по пунктам опроса. */
  content: string;
  /** Конкретные шаги: что делать, в каком порядке, с какой периодичностью (без выдуманных фактов). */
  recommendations: string[];
};

export type ForecastPayload = {
  sections: ForecastSection[];
  methodology_note: string;
};

export const FORECAST_JSON_INSTRUCTION = `Верни ТОЛЬКО валидный JSON объекта со структурой:
{
  "sections": [
    { "title": "строка", "content": "строка", "recommendations": ["строка", "..."] }
  ],
  "methodology_note": "строка"
}

Требования к sections (минимум 7 блоков, строго в таком порядке заголовков):
1. "Здоровье и соматический ресурс"
2. "Финансы и предсказуемость дохода"
3. "Работа, нагрузка и профессиональная траектория"
4. "Отношения и социальная среда"
5. "Психическое состояние, стресс и регуляция"
6. "Быт, структура и самоорганизация"
7. "Риски на горизонте 24 месяцев"

Можно добавить до 2 дополнительных блоков с нейтральными заголовками, если данные опроса это оправдывают.

Поле content (на каждый блок):
- 4–7 предложений: условный прогноз, механизмы (почему так может развиваться при текущих паттернах), зоны неопределённости.
- Запрещено: дословно перечислять ответы опросника, фразы вида «вы ответили да/нет на вопрос о…», бинарные выводы «у вас есть проблема / нет проблемы» без нюанса.
- Разрешено: обобщение нескольких пунктов в один вывод, оговорки по самоотчёту.

Поле recommendations (на каждый блок):
- Массив из ровно 6 строк (не меньше и не больше 6).
- Каждая строка — одна отдельная рекомендация: глагол в повелительном или «зафиксируйте / введите / назначьте себе»; конкретное действие; где уместно — частота (раз в неделю, 2 недели подряд) или измеримый минимум (например «15 минут», «одна встреча»).
- Без общих лозунгов («будьте здоровы», «больше заботьтесь о себе»). Без назначения лекарств и дозировок; при соматике/психике — формулировки вроде «запишитесь к …» вместо диагноза.
- Рекомендации должны логически вытекать из того же блока content и из профиля ответов, но не дублировать content дословно.

Стиль: холодный, без романтизации и вдохновляющих клише. Явно указывай неопределённость прогноза. Не диагностируй расстройства. Не обещай конкретных внешних событий (брак, выигрыш).

methodology_note: 2–3 предложения о том, что это вероятностная модель по самоотчёту, не медицинское заключение и не финансовая консультация; рекомендации — ориентиры, не индивидуальный план лечения.`;
